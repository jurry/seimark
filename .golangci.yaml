version: "2"
linters:
  default: standard
  enable:
    - errorlint
    - gocritic
    - misspell
    - nolintlint
    - prealloc
    - unconvert
    - unparam
formatters:
  enable:
    - gofmt
    - goimports
